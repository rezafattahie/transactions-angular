# Testing

## Unit & Component
Run:
- `npm test` (or your configured test command)

Coverage focus:
- repositories: correct API calls
- store: pagination + CRUD + error handling
- components: login/signup + tx table + modal

## E2E
Run:
- `npm run e2e`

Playwright tests intercept network requests and mock Backendless responses.
